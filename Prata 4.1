#include <iostream>
#include <string>


using namespace std;

int main()
{   
    string imie;
    string nazwisko;
    int wiek;
    int ocena;
    std::cout << "Jak masz na imie?" << std::endl;
    getline(cin, imie);
    std::cout << "Jak masz na nazwisko?" << std::endl;
    getline(cin, nazwisko);
    std::cout << "Ile masz lat?" << std::endl;
    cin >> wiek;
    std::cout << "Jaka chcesz ocene?" << std::endl;
    cin >> ocena;
    ocena = ocena - 1;
    cout << imie << ", " << nazwisko << ", " << wiek << ", " << ocena << "." << endl;
  return 0;  
}
